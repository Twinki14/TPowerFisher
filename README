Move the TPowerFisher directory to AlpacaBot\Scripts\Local

All profiles are found in the Profiles sub-directory, be sure to edit the default one (TPF), and be sure to select it when starting the script